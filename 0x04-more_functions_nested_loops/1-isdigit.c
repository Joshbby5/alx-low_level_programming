#include "main.h"

/**
 * _isdigit - Check for digits (0 through 9)
 *Return: 1 if digit, else 0
 */

int _isdigit(int c)
{
	char c;

	for (c > 47; c < 58)
		
	putchar('\n');
	return (1);	
	return (0);

}
